// Bubble Sort
// Complexity: time - O(n ^ 2)

void bubblesort(vector<int> & a) {
  for (int i = 1; i < a.size(); i++) {
    for (int j = 0; j < a.size() - i; j++) {
      if (a[j] > a[j + 1]) {
        swap(a[j], a[j + 1]);
      }
    }
  }
}
